//! Network / map events (landslide, festival, flood, …).
//!
//! MVP: empty stub resource. Future slices schedule rolls and emit commands.

use bevy_ecs::prelude::Resource;

/// Placeholder for future event scripting against the rail network.
#[derive(Debug, Default, Clone, Resource)]
pub struct EventDirector;
